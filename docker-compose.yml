version: '3'

services:
  zabbix-glpi-reporter:
    build: .
    container_name: relatorio_zabbix_glpi
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
